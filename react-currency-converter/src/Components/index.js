import Input from './Input'
//If we have many more components then then there is no need to export that components again and again 
//Otherwise we can create a another file and export components from here.
export{Input}